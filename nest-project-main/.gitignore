# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Secrets - each app keeps its own .env, never commit them
.env
.env.local
.env.*.local

# Dependencies and build output are ignored by each app's own .gitignore
# (api/.gitignore, web/.gitignore), so they do not need repeating here.
